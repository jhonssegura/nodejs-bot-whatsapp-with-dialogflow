const { firstClient } = require('../../queues');

module.exports = {
    /*
    entrance: async (req, res) => {
        const { to, text } = req.body;
        await firstClient.add({ to, text });
        res.send(`${to} has been added to the first queue`);
    },
    */
};