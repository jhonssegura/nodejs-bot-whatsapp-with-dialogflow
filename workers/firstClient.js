module.exports = (job, done) => {
    
    const { firstClient } = require('../queues');
    /*
    try {
        const { to, text } = job.data;
        // if (age < 18) throw new Error('You cannor access');
        firstClient.add({ to, text });
        job.progress(100);
        done(null, job.data);
    } catch (error) {
        done(error);
    }
    */
};